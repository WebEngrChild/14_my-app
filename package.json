{
  "name": "my-app",
  "private": true,
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter web dev",
    "build": "bun --filter web build",
    "lint": "bun --filter web lint",
    "mock": "bun --filter web mock",
    "api:generate": "bun --filter web api:generate"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "drizzle-kit": "^0.31.10",
    "supabase": "^2.116.0"
  }
}
